import type { SxProps } from '@mui/material';

const wrapper: SxProps = {
  alignItems: 'center',
  textAlign: 'center',
};

const title: SxProps = {
  mb: 2.5,
};

const chipsWrapper: SxProps = {
  mt: 2.5,
  mb: 5,
};

export const styles = {
  wrapper,
  title,
  chipsWrapper,
};
