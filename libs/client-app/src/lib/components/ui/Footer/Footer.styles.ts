import type { SxProps } from '@mui/material';

const container: SxProps = {
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
};

const logo: SxProps = {
  width: 49,
  height: 55,
};

export const styles = {
  container,
  logo,
};
