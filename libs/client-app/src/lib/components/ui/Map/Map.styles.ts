import type { SxProps } from '@mui/material';

const wrapper: SxProps = {
  aspectRatio: '1180 / 492',
  '& > div': {
    width: '100%',
    height: '100%',
  },
};

export const styles = {
  wrapper,
};
