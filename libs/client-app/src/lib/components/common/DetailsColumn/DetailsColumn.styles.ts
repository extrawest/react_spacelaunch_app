import type { SxProps } from '@mui/material';

const wrapper: SxProps = {
  alignItems: 'center',
  textAlign: 'center',
};

const detailValue: SxProps = {
  fontWeight: 700,
};

export const styles = {
  wrapper,
  detailValue,
};
